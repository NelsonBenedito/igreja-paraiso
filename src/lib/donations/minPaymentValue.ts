/**
 * Valor mínimo em reais quando o pedido envia `value` no POST de links públicos
 * (Asaas / regra da API Nest). Abaixo disto o parceiro não gera link de cobrança.
 */
export const MIN_PAYMENT_LINK_VALUE_BRL = 5;
